<div class="d-flex justify-content-between align-items-center mb-4">
  <h1>Clients</h1>
  <%= link_to "New Client", new_client_path, class: "btn btn-primary" %>
</div>

<% if @clients.any? %>
  <div class="table-responsive">
    <table class="table table-striped table-hover">
      <thead>
        <tr>
          <th>Name</th>
          <th>Email</th>
          <th>Phone</th>
          <th>Company</th>
          <th>Actions</th>
        </tr>
      </thead>
      <tbody>
        <% @clients.each do |client| %>
          <tr>
            <td><%= link_to client.name, client %></td>
            <td><%= client.email %></td>
            <td><%= client.phone %></td>
            <td><%= client.company %></td>
            <td>
              <%= link_to "View", client, class: "btn btn-sm btn-info" %>
              <%= link_to "Edit", edit_client_path(client), class: "btn btn-sm btn-warning" %>
              <%= button_to "Delete", client, method: :delete,
                  data: { confirm: "Are you sure?" },
                  class: "btn btn-sm btn-danger" %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
<% else %>
  <div class="alert alert-info">
    <p class="mb-0">No clients yet. <%= link_to "Create your first client", new_client_path %></p>
  </div>
<% end %>
